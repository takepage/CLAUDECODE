import { useState } from 'react';
import { Sparkles, Upload, TrendingUp, AlertCircle, Target, Wand2 } from 'lucide-react';

export default function WODStrategy() {
  const [wodInput, setWodInput] = useState('');
  const [wodImage, setWodImage] = useState<string | null>(null);
  const [wodType, setWodType] = useState<'FOR TIME' | 'AMRAP' | 'EMOM' | 'TABATA' | 'CUSTOM'>('FOR TIME');
  const [rxdWeight, setRxdWeight] = useState('');
  const [myWeight, setMyWeight] = useState('');
  const [wodName, setWodName] = useState('');
  const [autoGeneratedName, setAutoGeneratedName] = useState('');
  const [analysis, setAnalysis] = useState<any>(null);
  const [isAnalyzing, setIsAnalyzing] = useState(false);

  const handleImageUpload = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (file) {
      const reader = new FileReader();
      reader.onloadend = () => {
        setWodImage(reader.result as string);
      };
      reader.readAsDataURL(file);
    }
  };

  const handleAutoGenerateName = () => {
    // AI로 자동 생성 (시뮬레이션)
    const suggestions = [
      'Deadlift HSPU Sprint',
      'The Shoulder Burner',
      'Heavy Deadlift Complex',
      'Cardio Crusher',
      'Power & Speed'
    ];
    const randomName = suggestions[Math.floor(Math.random() * suggestions.length)];
    setAutoGeneratedName(randomName);
    setWodName(randomName);
  };

  const handleAnalyze = () => {
    if (!wodInput.trim()) return;

    setIsAnalyzing(true);

    setTimeout(() => {
      setAnalysis({
        scaling: {
          deadlift: myWeight || '95kg (60% of 1RM)',
          hspu: 'Abmat HSPU',
        },
        pacing: [
          '21 Reps: 2-3세트로 분할, 3분 목표',
          '15 Reps: 2세트로 분할, 2분 목표',
          '9 Reps: Unbroken 시도, 1분 30초 목표',
        ],
        bottleneck: 'HSPU가 주요 병목 구간입니다. 어깨 피로도 관리가 핵심입니다.',
        estimatedTime: '8-10분',
      });
      setIsAnalyzing(false);
    }, 1500);
  };

  return (
    <div>
      <div className="flex items-center gap-3 mb-8">
        <div className="p-3 rounded-xl bg-primary-light">
          <Sparkles className="w-6 h-6 text-primary" />
        </div>
        <div>
          <h2 className="text-2xl font-bold text-text-primary">WOD 전략 분석</h2>
          <p className="text-sm text-text-secondary">오늘의 WOD를 입력하고 AI 맞춤 전략을 받아보세요</p>
        </div>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        {/* 왼쪽: WOD 입력 */}
        <div className="space-y-6">
          {/* WOD 타입 선택 */}
          <div className="card p-6">
            <h3 className="text-lg font-semibold text-text-primary mb-4">WOD 타입</h3>
            <div className="flex flex-wrap gap-3">
              {(['FOR TIME', 'AMRAP', 'EMOM', 'TABATA', 'CUSTOM'] as const).map((type) => (
                <button
                  key={type}
                  onClick={() => setWodType(type)}
                  className={`px-4 py-2 rounded-xl font-semibold transition-all ${
                    wodType === type
                      ? 'bg-primary text-white shadow-md'
                      : 'bg-light-bg text-text-secondary hover:bg-light-card-hover'
                  }`}
                >
                  {type}
                </button>
              ))}
            </div>
          </div>

          {/* WOD 입력 (텍스트 또는 이미지) */}
          <div className="card p-6">
            <h3 className="text-lg font-semibold text-text-primary mb-4">WOD 내용</h3>

            {/* 이미지 업로드 */}
            <div className="mb-4">
              <label className="block w-full">
                <div className="border-2 border-dashed border-light-border rounded-xl p-8 text-center hover:border-primary hover:bg-primary-light/20 transition-colors cursor-pointer">
                  {wodImage ? (
                    <div className="relative">
                      <img src={wodImage} alt="WOD" className="max-h-48 mx-auto rounded-lg" />
                      <button
                        onClick={(e) => {
                          e.preventDefault();
                          setWodImage(null);
                        }}
                        className="absolute top-2 right-2 px-3 py-1 bg-red-500 text-white rounded-lg text-sm"
                      >
                        삭제
                      </button>
                    </div>
                  ) : (
                    <>
                      <Upload className="w-8 h-8 text-text-tertiary mx-auto mb-2" />
                      <p className="text-sm text-text-secondary">이미지를 클릭하여 업로드</p>
                      <p className="text-xs text-text-tertiary mt-1">또는 아래에 직접 입력하세요</p>
                    </>
                  )}
                </div>
                <input
                  type="file"
                  accept="image/*"
                  onChange={handleImageUpload}
                  className="hidden"
                />
              </label>
            </div>

            {/* 텍스트 입력 */}
            <div>
              <label className="block text-sm font-medium text-text-primary mb-2">
                WOD 설명 (직접 입력)
              </label>
              <textarea
                value={wodInput}
                onChange={(e) => setWodInput(e.target.value)}
                placeholder="예: 21-15-9&#10;Deadlift 225lb&#10;HSPU"
                className="w-full px-4 py-3 rounded-xl border border-light-border bg-white text-text-primary placeholder-text-tertiary resize-none focus:outline-none focus:ring-2 focus:ring-primary"
                rows={6}
              />
            </div>
          </div>

          {/* RXD 무게 및 본인 무게 */}
          <div className="card p-6">
            <h3 className="text-lg font-semibold text-text-primary mb-4">무게 설정</h3>
            <div className="grid grid-cols-2 gap-4">
              <div>
                <label className="block text-sm font-medium text-text-primary mb-2">RXD 무게</label>
                <input
                  type="text"
                  value={rxdWeight}
                  onChange={(e) => setRxdWeight(e.target.value)}
                  placeholder="225lb / 102kg"
                  className="w-full px-4 py-2 rounded-xl border border-light-border bg-white focus:outline-none focus:ring-2 focus:ring-primary"
                />
              </div>
              <div>
                <label className="block text-sm font-medium text-text-primary mb-2">본인 무게</label>
                <input
                  type="text"
                  value={myWeight}
                  onChange={(e) => setMyWeight(e.target.value)}
                  placeholder="185lb / 84kg"
                  className="w-full px-4 py-2 rounded-xl border border-light-border bg-white focus:outline-none focus:ring-2 focus:ring-primary"
                />
              </div>
            </div>
          </div>

          {/* WOD 이름 */}
          <div className="card p-6">
            <h3 className="text-lg font-semibold text-text-primary mb-4">WOD 이름</h3>
            <div className="flex gap-3">
              <input
                type="text"
                value={wodName}
                onChange={(e) => setWodName(e.target.value)}
                placeholder="WOD 이름 (선택사항)"
                className="flex-1 px-4 py-2 rounded-xl border border-light-border bg-white focus:outline-none focus:ring-2 focus:ring-primary"
              />
              <button
                onClick={handleAutoGenerateName}
                className="px-4 py-2 bg-secondary-light text-secondary rounded-xl font-semibold hover:bg-secondary hover:text-white transition-colors flex items-center gap-2"
              >
                <Wand2 className="w-4 h-4" />
                자동생성
              </button>
            </div>
            {autoGeneratedName && (
              <p className="text-xs text-text-tertiary mt-2">
                💡 추천: {autoGeneratedName}
              </p>
            )}
          </div>

          {/* 분석 버튼 */}
          <button
            onClick={handleAnalyze}
            disabled={!wodInput.trim() || isAnalyzing}
            className="w-full px-6 py-4 bg-primary text-white rounded-xl font-bold text-lg hover:bg-primary-dark transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-2 shadow-md"
          >
            {isAnalyzing ? (
              <>
                <div className="w-5 h-5 border-2 border-white border-t-transparent rounded-full animate-spin"></div>
                AI 분석 중...
              </>
            ) : (
              <>
                <Sparkles className="w-5 h-5" />
                AI 전략 분석하기
              </>
            )}
          </button>
        </div>

        {/* 오른쪽: 분석 결과 */}
        <div className="space-y-6">
          {analysis ? (
            <>
              <div className="card p-6">
                <h3 className="text-lg font-bold text-text-primary mb-4">분석 결과</h3>

                {/* Scaling */}
                <div className="mb-4 p-4 rounded-xl bg-secondary-light border border-secondary/20">
                  <div className="flex items-center gap-2 mb-3">
                    <Target className="w-5 h-5 text-secondary" />
                    <h5 className="font-semibold text-text-primary">추천 무게/스케일링</h5>
                  </div>
                  <ul className="space-y-2 ml-7">
                    <li className="text-sm text-text-secondary">
                      Deadlift: <span className="font-semibold text-text-primary">{analysis.scaling.deadlift}</span>
                    </li>
                    <li className="text-sm text-text-secondary">
                      HSPU: <span className="font-semibold text-text-primary">{analysis.scaling.hspu}</span>
                    </li>
                  </ul>
                </div>

                {/* Pacing */}
                <div className="mb-4 p-4 rounded-xl bg-primary-light border border-primary/20">
                  <div className="flex items-center gap-2 mb-3">
                    <TrendingUp className="w-5 h-5 text-primary" />
                    <h5 className="font-semibold text-text-primary">페이싱 & 렙 스킴</h5>
                  </div>
                  <ul className="space-y-2 ml-7">
                    {analysis.pacing.map((item: string, index: number) => (
                      <li key={index} className="text-sm text-text-secondary">
                        {item}
                      </li>
                    ))}
                  </ul>
                  <div className="mt-3 ml-7 text-xs text-text-tertiary">
                    예상 시간: <span className="font-semibold text-primary">{analysis.estimatedTime}</span>
                  </div>
                </div>

                {/* Bottleneck */}
                <div className="p-4 rounded-xl bg-accent-orange/10 border border-accent-orange/20">
                  <div className="flex items-center gap-2 mb-3">
                    <AlertCircle className="w-5 h-5 text-accent-orange" />
                    <h5 className="font-semibold text-text-primary">주의사항 (Bottleneck)</h5>
                  </div>
                  <p className="text-sm text-text-secondary ml-7">
                    {analysis.bottleneck}
                  </p>
                </div>
              </div>

              {/* 기록하기 버튼 */}
              <button className="w-full px-6 py-3 bg-secondary text-white rounded-xl font-semibold hover:bg-secondary-dark transition-colors">
                이 전략으로 운동 기록하기
              </button>
            </>
          ) : (
            <div className="card p-12 text-center">
              <div className="w-20 h-20 bg-primary-light rounded-full flex items-center justify-center mx-auto mb-4">
                <Sparkles className="w-10 h-10 text-primary" />
              </div>
              <h3 className="text-lg font-bold text-text-primary mb-2">AI 분석 대기 중</h3>
              <p className="text-text-secondary">
                WOD를 입력하고 분석 버튼을 클릭하세요.
              </p>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
